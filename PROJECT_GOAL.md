# 动态 Galgame 项目目标

> 本文档描述项目的目标产品定义，是后续架构设计、功能取舍和验收工作的基准，不代表当前实现已经全部完成。

## 一句话定义

一个保持传统 Galgame 游玩流程、由 Agent 从作者提供的故事起点实时续写，并保证在有限流程内生成独一无二动态结局的游戏平台。

## 参与者

- 剧本作者或二创作者负责制作和发布剧本包。
- 玩家只负责选择剧本、开始游戏并在关键时刻选择选项。
- Agent 是本局游戏的实时编剧和导演。
- 引擎负责世界状态、一致性、收束和通过判定。

## 剧本能写什么

剧本只能定义游戏开始前已经成立的内容：

- 世界设定、题材和不可违反的规则。
- 角色身份、性格、语言风格和行为边界。
- 主角身份，玩家不能自行创建角色。
- 角色之间的初始关系。
- 游戏开始前发生的故事历史。
- 已经存在的事实、秘密和角色知识。
- 尚未确定、可以在游玩中动态确定的潜在真相。
- 游戏开场的位置、人物和局势。
- 作者定义的通过条件。
- 期望流程长度和最大场景数。
- 素材接口，首版可以没有实际素材。

## 剧本不能写什么

作者不能规定游戏开始后将要发生的内容：

- 不能写未来剧情大纲。
- 不能写必须发生的关键事件。
- 不能预设角色路线。
- 不能制作固定分支树。
- 不能预设好、坏、真或隐藏结局。
- 不能指定玩家最终必须和谁在一起。
- 不能规定第几幕必须发生什么。

作者提供的是一个完整、可续写的故事起点，而不是尚未播放的传统剧本。

## 实际游戏流程

```text
玩家选择剧本
-> 进入作者定义的开场
-> Agent 连续生成完整 Galgame 演出
-> 到关键决策点提供 2-4 个选项
-> 玩家选择主角的意图
-> 引擎记录选择及其真实影响
-> Agent 根据最新历史继续演出
-> 在有限场景内形成动态结局
-> 系统检查作者定义的通过条件
```

选项不会频繁出现。两次选择之间必须是一段连续、完整的 Galgame 演出，包括旁白、人物台词、主角内心和角色行动，不能变成聊天、小说摘要或状态报告。

## 动态结局

- 所有游戏都必须最终结束，不能无限发散。
- 结局内容由 Agent 根据整局经历现场生成。
- 相同剧本可以产生大量不同结局。
- 结局没有作者预设的 Good、Bad、True 分类。
- 剧情结束不代表通过。
- 满足作者要求则为“已通过”，没有满足则为“未通过”。
- 未通过也必须获得符合因果、完整收束的动态结局。
- Agent 可以描述结局的意义，但不能自行修改通过标准。

作者规定“什么算通过”，Agent 决定“具体怎样发展和怎样结束”。

## 收束机制

引擎不能依靠固定剧情节点收束，而要依靠：

- 最小和最大场景预算。
- 当前叙事阶段。
- 已开启、已解决和未解决的矛盾。
- 角色目标及关系变化。
- 剩余可用场景数。
- 后期禁止引入大型新矛盾。
- 接近上限时强制回收现有矛盾。
- 到达上限时生成动态结局，无论是否通过。

## 前后端体验

前端播放器和后端引擎必须解耦为异步生产者与消费者：

- 玩家选择后可以出现一次生成等待。
- 后端生成并验证“本次选择到下次选择”的演出片段。
- 前端从缓冲队列连续播放。
- 普通场景之间不能出现加载和网络停顿。
- 后端生成完成不等于玩家播放完成。
- 只有玩家读完所有缓冲内容后，才能显示下一个选项或结局。
- 素材系统后补，但背景、立绘、表情、BGM、音效和转场接口现在保留。

## 必须坚持的技术原则

- 世界真相、角色知识、角色信念和说出的台词必须分离。
- 角色不能凭空知道未见证或未被告知的信息。
- Agent 只能提出剧情和状态变化，不能直接篡改权威状态。
- Validator、Simulator、reducer 和 EventStore 负责检查与提交。
- 每次玩家选择必须产生可追踪的真实影响。
- 所有游戏过程可以回放、审计和自动评测。
- 流式播放不能绕过原本的 Planner 和状态约束。

## 产品本质

本项目不是聊天机器人、AI 小说续写器、自动生成 Ren'Py 的工具，也不是开放世界文字 RPG。

本项目要实现的是：

> 以传统 Galgame 为表现和交互形式，以作者剧本包为故事起点与规则，以玩家选项为唯一输入，以 Agent 的实时续写能力为核心，并由可靠引擎保证一致性和有限收束的动态 Galgame 平台。

## 成功标准

同一个剧本多次游玩能够产生明显不同、但始终符合世界设定、人物逻辑和 Galgame 节奏的故事，并且每一局都能在有限流程内形成完整结局。结局是否通过，只由剧本作者定义的通过条件决定。
